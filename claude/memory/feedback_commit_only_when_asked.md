---
name: Commit only when asked
description: Never commit or push unless the user explicitly asks
type: feedback
---

Never create git commits or push to remote unless the user explicitly asks.
Do not commit after making file changes, even if the changes are complete.
Wait for the user to say "commit", "commit and push", or similar.
